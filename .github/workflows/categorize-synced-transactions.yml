name: Categorize Synced Transactions

on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:

jobs:
  categorize:
    runs-on: ubuntu-latest

    steps:
      - name: Send request to categorize synced transactions
        env:
          CRON_SECRET: ${{ secrets.CRON_SECRET }}
        run: |
          if [ -z "$CRON_SECRET" ]; then
            echo "CRON_SECRET is not set"; exit 1
          fi
          status=$(curl -sS -o /tmp/response.json -w "%{http_code}" -X POST \
            --connect-timeout 15 \
            --max-time 90 \
            --retry 2 \
            --retry-all-errors \
            --retry-delay 5 \
            https://finance-tracker-plum-six.vercel.app/api/cronjob/categorize-synced-transactions \
            -H "Authorization: Bearer $CRON_SECRET")
          cat /tmp/response.json
          echo
          if [ "$status" -ne 200 ]; then exit 1; fi
